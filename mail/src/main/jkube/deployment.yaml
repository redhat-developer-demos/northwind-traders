metadata:
  annotations:
    app.openshift.io/connects-to: rabbit-mq
  labels:
    app.kubernetes.io/part-of: ${jkube.enricher.jkube-project-label.group}
    app.openshift.io/runtime: micronaut
# TODO - Raise an issue at JKube, since this should not be required
spec:
  template:
